name = "svar-marketing"
compatibility_date = "2024-08-01"
pages_build_output_dir = "out"

# Used only for local preview (`npm run pages:preview`).
# Git-connected Pages projects should NOT set a deploy command in the
# dashboard — Cloudflare deploys `out/` automatically after the build.
